---
icon: i-simple-icons-playwright
---

# Playwright

> Local Chromium sessions using `playwright`.

## Setup

```bash
npm i playwright
```

## Usage

```ts
import { createBrowser } from 'unagent/browser'

const browser = await createBrowser({
  provider: {
    name: 'playwright',
    launchOptions: { headless: true },
  },
})

const session = await browser.newSession()
const page = await session.newPage()
await page.goto('https://example.com')

const text = await page.extract({ kind: 'text', selector: 'h1' })
await session.close()
await browser.close()
```

## E2E Testing

Run local Playwright E2E (requires `playwright` installed locally):

```bash
pnpm playground:browser:e2e
```

Run the same E2E inside Docker (no local browser setup required):

```bash
pnpm playground:browser:e2e:docker
```

## Related

- [Browser](/features/browser)
- [All Features](/features)
- [Core](/core)
