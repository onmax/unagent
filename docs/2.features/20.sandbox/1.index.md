---
icon: i-lucide-box
---

# Sandbox

> Create sandboxed execution environments with a unified API.

## Usage

```ts
import { createSandbox } from 'unagent/sandbox'
```

## Quick Start

```ts
const sandbox = await createSandbox({
  provider: { name: 'vercel', runtime: 'node24', timeout: 300_000 },
})

const result = await sandbox.exec('node', ['-e', 'console.log("hello")'])
await sandbox.stop()
```

## Providers

- [Vercel](/features/sandbox/vercel)
- [Cloudflare](/features/sandbox/cloudflare)
- [Deno](/features/sandbox/deno)

## Related
- [All Features](/features)
- [All Modules](/modules)
